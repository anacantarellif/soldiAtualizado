// const connection = require('../config/db.js');
// const dotenv = require('dotenv').config();

// async function storeTask(request, response){
//     const params = Array(
//         request.body.title,
//         request.body.description
//     );

//     const query = "INSERT INTO usuário(title, description) VALUES(?,?)";

    
// }